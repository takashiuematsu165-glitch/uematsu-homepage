# 表示確認メモ

## 確認済み

- デスクトップ幅で Home、Profile、Contact、SNS の各ページが表示されることを確認した。
- 共通ヘッダー、現在位置のナビゲーション表示、フッター、カード構成が各ページで一貫している。
- Contact は6つの連絡手段を応答目安と利用状態付きで表示し、準備中の Discord を操作不能な状態として区別している。
- SNS は公式アカウントの説明、用途、アカウント名、リンク設定待ちの状態を一目で判別できる。

## 公開前に必要な作業

- App.tsx 内の Contact と SNS の「リンク設定待ち」項目に、実際のメールアドレスおよび各SNSのURLを設定する。
- 実URL設定後は、該当カードを外部リンクとして有効化する。

## CMS復元の確認

既存のmicroCMSへ接続し、実際の記事をホームの最新お知らせとお知らせ一覧の両方に表示できることを確認した。記事カードには公開日、タイトル、本文から作成した要約、個別記事へのリンクを表示する。個別記事画面は本文を安全に整形して表示する実装としている。

個別記事についても、実際のCMS記事で公開日、見出し、本文、リスト、一覧へ戻るリンクが正しく表示されることを確認した。非同期に追加される記事コンテンツにも表示アニメーションが適用されるよう、監視処理を補強している。

## カテゴリー・SNS・上部へ戻る操作の確認

microCMSのカテゴリーテーブルから「更新情報」を取得し、Newsページには「すべて」「更新情報」「未分類」の絞り込みを表示した。現在の記事は未分類のため、未分類フィルターにも適切に含まれる。スマートフォン幅では各フィルターと記事カードが読みやすく折り返され、SNSページはInstagram、X、Facebook、Threads、TikTok、YouTube、LINE、Discord、note、GitHub、LinkedIn、Portfolioを二つのグループで表示する。全ページに追加したBack to Topボタンは、一定量スクロールした後に表示され、動きを減らす設定では即時に先頭へ戻る実装としている。

## プライバシーポリシーの確認

ヘッダーのナビゲーションは変更せず、フッターにのみPrivacy Policyのリンクを追加した。専用ページでは、情報の取得・利用目的・アクセス解析・外部サービス・第三者提供・安全管理・問い合わせ先・変更の扱いを掲載している。デスクトップおよびスマートフォン幅で、見出し、本文、フッター導線が読みやすく表示されることを確認した。

## Cookie同意機能の確認

Cookie同意バナーはデスクトップとスマートフォンで画面下部に表示され、同意・拒否・プライバシーポリシーへの導線を提供する。選択結果はパスをサイト全体に設定したJavaScript Cookieに1年間保存し、次回以降はバナーを表示しない。フッターのCookie設定からバナーを再表示でき、同意を選んだときだけアクセス解析スクリプトを動的に読み込む。保存方式と解析読み込みはブラウザだけで完結するため、静的ファイルを配信できる一般的なホスティング環境に依存しない。

## プライバシーポリシー見出しの改行修正

参照画像と同じ横長画面で、プライバシーポリシーの見出しが一行に収まることを確認した。見出し用の列を広げ、改行不可の設定をページ固有に適用している。スマートフォン幅では文字サイズを調整し、はみ出さず一行で読めることを確認した。

## Google アナリティクスの同意制御

測定ID G-S6GMRTWF52をGoogle アナリティクス用に設定した。初期HTMLにはGoogle タグを埋め込まず、Cookieで「同意する」を選択した場合に限り、ブラウザ上でGoogle タグを動的に追加する。拒否・未選択時はタグを追加せず、設定変更で拒否を選んだ場合はGoogle アナリティクスの同意を拒否へ更新して既存のGoogle アナリティクスCookieを削除する。Cookieバナーの文言はデスクトップ・スマートフォン表示で確認済み。

## クリックイベントとCookieバナー改善

Cookie同意済みでGoogle タグが読み込まれた場合だけ、`contact_page_click`、`contact_method_click`、`social_link_click`の各イベントを送信する。Contactページのナビゲーション導線は表示位置を、連絡先カードは連絡手段名を、SNSリンクはSNS名をイベントパラメータとして送信する。未同意・拒否時はイベント処理を終了するため、Google アナリティクスへクリックイベントを送信しない。Cookieバナーには、シールドアイコン、淡いブルー・ピンクのガラス調レイヤー、独立したプライバシーポリシーリンクを追加し、デスクトップとスマートフォン幅で表示を確認した。

## デザイン維持の整形方針

ホームとCookieバナーをデスクトップ・スマートフォンで確認した。既存の淡いブルー、ピンク、ガラス調の表現、カード形状、ボタンの配色は変更せず、Cookieバナーの内側の余白、文章の最大幅、リンクと選択ボタンの整列のみを整える。これにより、見た目の方向性を保ったまま文章の読み取りやすさと操作のまとまりを改善する。

整形後も、デスクトップではCookieバナーの文章、プライバシーポリシーリンク、選択ボタンが視線の流れに沿って整列している。スマートフォンでは文章幅、リンク、二つの選択ボタンが過度に詰まらず、既存のカード・配色・ガラス調の雰囲気を保っていることを確認した。

## Google アナリティクス実ブラウザ検証

開発サーバーを実ブラウザで開き、Cookieの状態を変えて検証した。未同意では測定ID G-S6GMRTWF52のGoogle タグ要求、タグ要素、`gtag`関数のいずれも確認されなかった。同意済みでは同じ測定IDのタグ要求・タグ要素・`gtag`関数を確認し、`contact_page_click`および`social_link_click`イベントがデータレイヤーに追加されることを確認した。拒否時にはタグ要求、タグ要素、`gtag`関数はいずれも確認されなかった。

## GitHub Pages公開準備

GitHubリポジトリ `takashiuematsu165-glitch/uematsu-homepage` を作成し、mainブランチのサイトコードとgh-pagesブランチの静的ビルドを反映した。GitHub API経由のPages有効化は統合トークンの権限不足で拒否されたため、リポジトリのPages設定画面でブランチ公開を有効化する手順へ切り替えた。設定画面でgh-pagesブランチが反映されるまでの短時間は、GitHub側の同期待ちが発生する場合がある。

GitHub Pagesはgh-pagesブランチのルートを公開元として有効化され、公開URLは `https://takashiuematsu165-glitch.github.io/uematsu-homepage/` となった。公開URLを実際に開き、ホーム画面、公開用に複製した画像、Cookieバナー、microCMSのお知らせ表示が正常に表示されることを確認した。

## ユーザーブラウザでのGoogle アナリティクス確認

ユーザーブラウザでCookie同意後にContactページへのヘッダー導線をクリックし、指定されたGoogle アナリティクスのウェブストリーム画面を確認した。ストリームの測定IDは実装済みの `G-S6GMRTWF52` と一致している。一方、確認時点では管理画面に「過去48時間に受信したデータはありません」「アクティブユーザー0」「イベント数0」と表示されており、外部のGoogle アナリティクス側ではまだ受信を確認できていない。実装上のタグ読み込みとイベントキューはローカル検証で確認済みだが、公開環境からGoogleの収集エンドポイントまで到達しているかを追加で調査する必要がある。

キャッシュを回避するクエリ付きURLでも、Cookie同意済みのユーザーブラウザからContact導線のクリックを再実施した。Google アナリティクス受信画面では引き続きデータを確認できていないため、ブラウザ側または検証環境側でGoogleタグ通信がブロックされている可能性を調査している。

Google公式Tag Assistantをユーザーブラウザで接続して確認したところ、公開サイトでは測定ID `G-S6GMRTWF52` のGoogleタグが検出され、`gtag('config')`、同意初期化、ページビューの送信が記録された。したがって、公開サイトのGoogleタグはブラウザ上で正常に機能していることを確認できた。管理画面の集計表示には反映まで時間差が生じる場合がある。

GitHub Pages向けのハッシュルーティングではContactリンクが `#/contact` となるため、旧来の `/contact` だけを判定する実装ではContactイベントが発火しないことを特定した。両形式を判定するよう修正し、公開コードのテストで `contact_page_click` と、実URL設定後のSNSリンクに適用される `social_link_click` がデータレイヤーへ追加されることを確認した。サンドボックス側の自動通信検証では `googletagmanager.com` が `ERR_BLOCKED_BY_CLIENT` でブロックされるが、ユーザーブラウザ上のTag AssistantではGoogleタグとページビューを確認できている。

## GitHub画像圧縮の確認

ブランドマークとホーム背景をWebP形式で確認し、ロゴの輪郭・色、ホーム背景の淡いグラデーション・球体表現が保たれていることを確認した。ブランドマークは約2.64MBから約0.56MB、ホーム背景は約2.97MBから約0.02MBへ削減できている。

Contact・SNSページ用の抽象モチーフも確認し、淡いブルー、ピンク、黄色のアクセント、背景の余白感、細いラインが保たれていることを確認した。Contactモチーフは約3.11MBから約0.09MB、SNSモチーフは約3.26MBから約0.05MBへ削減できている。

GitHub Pagesへ圧縮版を反映後、公開URLからHome、Profile、News、SNS & Links、Contactのナビゲーションおよびホームのお知らせ・プロフィール導線を取得できることを確認した。公開ブラウザのスクリーンショット取得は接続タイムアウトとなったため、別途ローカルのプレビューで視覚確認を行う。

ローカルプレビューでは、ホームのブランドマークとヒーロー背景、SNSの背景モチーフ、Contactの背景モチーフが圧縮後も表示され、レイアウトと既存の淡いブルー・ガラス調デザインに崩れがないことを確認した。

WordPress.com連携の設定を確認したところ、WordPressコネクターは無効（`enabled: false`）であり、画像保存やサイト公開には使用されない状態であることを確認した。

GitHub Pagesの各WebPファイル（ブランドマーク、ホーム、SNS、Contactの4画像）に対してHTTP応答を確認し、すべて `200` と `content-type: image/webp` で配信されていることを確認した。

ホームヒーロー右側のプロフィール用カードを撤去し、本文パネルと淡い背景モチーフのみが残る構成へ調整した。デスクトップとスマートフォンで可読性・余白・既存のガラス調表現が維持されることを確認し、GitHub Pagesの公開URLでも右側カードが表示されないことを確認した。

カード撤去後に残った右側の空白感を解消するため、ヒーローパネルをコンテンツ幅いっぱいまで広げ、右側に淡いブルー・ピンクの放射状グラデーションを重ねた。デスクトップ、スマートフォン、GitHub Pages公開版で、不要な独立カードや不自然な余白がなく、既存デザインに沿った表示であることを確認した。

Google アナリティクスの詳細計測として、Cookie同意済みの場合だけ、ハッシュルーティングに対応したページ閲覧、25・50・75・90%のスクロール到達、ヘッダー・フッターのナビゲーション、ホームのプロフィール導線、お知らせ記事、Contact、SNS、外部リンク、Back to Topの各イベントを送信するよう追加した。公開版の自動検証では、ページ閲覧、スクロール到達、主要導線、Contact、SNS、外部リンクの全イベントがデータレイヤーに記録され、Cookie拒否時はGoogleタグ・`gtag`・イベントのいずれも生成されないことを確認した。自動検証環境ではGoogleの収集通信が `ERR_BLOCKED_BY_CLIENT` となるが、これは既知の検証環境によるブロックであり、ユーザーブラウザ上のTag AssistantでGoogleタグとページビューを確認済みである。

プロフィールページでは、本文カードだけがページ全幅の左側に寄って見えることで右側に大きな空白感が生じていた。プロフィール専用の本文幅を設定し、カードを適正幅で中央配置した。デスクトップ・スマートフォン・GitHub Pages公開版で、内容のまとまりと可読性が保たれ、不要な右側の空白感が解消されていることを確認した。

Contactページのメールアドレスは初期HTML・JavaScript・GitHub Pages公開ファイルに含めず、Cloudflare Workerの暗号化シークレットとして保持する構成に変更した。公開版では認証前にメールアドレスを表示しないこと、Workerのメール取得APIが認証トークンなしで `401` を返すこと、認証トークンがない検証APIが `400` を返すこと、許可されないOriginが `403` を返すことを確認した。reCAPTCHAウィジェットは認証開始時だけ読み込み・表示されることを確認した。自動検証ブラウザでは第三者サービスの通信制限によりreCAPTCHAのエラー通知が出る場合があるが、メールアドレスは非表示のまま維持される。

reCAPTCHAキーがEnterprise v3キーであることを確認し、チェックボックス方式から `email_reveal` アクションの非表示トークン方式へ切り替えた。Cloudflare WorkerはGoogle CloudのCreateAssessment APIで、トークンの有効性、ホスト名、アクション、リスクスコアを確認する。Google CloudプロジェクトIDとreCAPTCHA Enterprise API専用に制限したAPIキーはCloudflare Secretとして保持し、GitHub Pagesには含めない。ユーザーブラウザの公開Contactページで、認証前はメールアドレスが非表示であること、ボタン操作後に評価が完了して `uematsu_kouki@yahoo.co.jp` とコピー用ボタンが表示されること、コピー成功メッセージが出ることを確認した。

アクセシビリティ改善として、キーボード利用者が最初に到達できる「本文へ移動」スキップリンク、明確なフォーカスリング、読みやすい文字表示の切り替え、切り替え結果を伝えるライブリージョン、コントラストを高めるOS設定への対応を追加した。既存の動きを減らす設定への配慮も維持している。デスクトップ・スマートフォンで既存のレイアウトを確認し、キーボードでスキップリンクを実行してもハッシュルーターが404へ遷移せず、ホーム画面の主要コンテンツへ移動することを確認した。

## フッターのアクセシビリティメニュー確認

フッターの操作を「アクセシビリティメニュー」ボタンへ集約した。デスクトップ幅ではフッターのリンク群と並び、淡いガラス調のボタンとして既存の配色・余白に馴染むことを確認した。スマートフォン幅でも、ナビゲーションリンク、Cookie設定、アクセシビリティメニューの順で自然に折り返され、横方向のはみ出しがないことを確認した。

メニューには文字サイズ（小・標準・大）、コントラスト強調、動きの停止、リンク下線、行間、文字間隔、フォーカス表示の各設定と、標準状態に戻す操作を実装した。各設定はボタンの押下状態とオン・オフ表示で伝わり、ライブリージョンおよびブラウザのローカル保存によって、選択内容の通知と次回訪問時の復元に対応している。

## ページ遷移時の上部表示確認

ハッシュルーティングでホーム、プロフィール、お知らせへ移動した際、移動先のページ見出しが表示される上部位置へ戻ることを確認した。ブラウザによるスクロール位置の復元を無効化し、ルート変更直後・描画完了後・短時間後の複数のタイミングで上部位置を適用することで、リンク操作と直接URL遷移のどちらでも前ページのスクロール位置を引き継がない構成にしている。
